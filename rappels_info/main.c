#include "fonctions.h"
#include <time.h>

int main() {
    srand(time(NULL));  // Initialiser le générateur de nombres aléatoires

    //boucles_algos_simples();
    //manip_pointeurs();
    //alloc_memoire();
    manip_fichiers();

    return 0;
}
